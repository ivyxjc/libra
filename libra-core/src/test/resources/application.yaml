spring:
    datasource:
        url: jdbc:h2:mem::LIBRA;MODE=MySQL
        username: user
        password: password
        schema: classpath:TestInit.sql

mybatis:
    type-aliases-package: com.ivyxjc.libra.core.dao
    mapper-locations: classpath:mybatis/*.xml